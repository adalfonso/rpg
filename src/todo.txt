// cleanup when scene ends to remove fixtures from the event bus
// slow player when moving diagonally (75%)
// scale inventory items
// state not working properly
// resolve what name, class, ref, etc represent
// resolve equipped items
// saving commits but doesnt force save. if we change how the state for something
   is calculated during deve it wont forcefully write the new data
// battle animation used config resolution instead of current resolution
// align battle menu to player
// scale battle
